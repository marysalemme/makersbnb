<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Makers BNB</title>
    <style>
      .topnav {
        background-color: #333;
        overflow: hidden;
      }
      .topnav a {
        float: left;
        display: block;
        color: #f2f2f2;
        text-align: center;
        padding: 14px 16px;
        text-decoration: none;
        font-size: 17px;
      }
      .topnav a:hover {
        background-color: #ddd;
        color: black;
      }
      #signed_in {
        float: right;
        display: block;
        text-align: center;
        color: white;
        margin-right: 10px;
        font-size: 14px;
      }
      #sign_out {
        float: right;
        color: white;
        margin-right: 10px;
        font-size: 14px;
        padding: 14px 16px;
      }
    </style>
  </head>

  <body>
    <% if current_user %>
      <div class="topnav" id="myTopnav">
        <a href="/">Home</a>
        <a href="/users/spaces">My spaces</a>
        <a href="/space/new">Add a space</a>
        <a href="/space">Find a space</a>
        <div>
          <form id="sign_out" method="post" action="/sessions">
            <input type="hidden" name="_method" value="delete">
            <input type="submit" value="Sign Out">
          </form>
        </div>
        <div>
          <p id="signed_in"> Signed in as <%= current_user.username %></p>
        </div>
      </div>
    <% else %>
      <div class="topnav" id="myTopnav">
        <a href="/">Home</a>
        <a href="/sessions/new">Log in</a>
        <a href="/users/new">New user?</a>
      </div>
    <% end %>

    <% if flash[:errors] && !flash[:errors].empty? %>
      <% flash[:errors].each do |error| %>
        <div class="alert">
          <%= error %>
        </div>
      <% end %>
    <% end %>

    <%= yield %>
  </body>
</html>
